//SET

#include<iostream>
using namespace std;
int main(){
    set<int>s;

    s.inset(5);
    s.inset(5);
    s.inset(5);
    s.inset(7);
    s.inset(6);
    s.inset(9);

    for (auto i:5){
        cout<<i<<endl;
    }
    
    set<int>::iterator 
}